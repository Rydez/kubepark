# PersistentVolume for Attraction
apiVersion: v1
kind: PersistentVolume
metadata:
  name: ${ATTRACTION_TYPE}-pv-${INSTANCE_ID}
  labels:
    app.kubernetes.io/name: kubepark
    app.kubernetes.io/component: attraction
    app.kubernetes.io/instance: ${ATTRACTION_TYPE}-${INSTANCE_ID}
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  hostPath:
    path: /tmp/kubepark/attractions/${ATTRACTION_TYPE}-${INSTANCE_ID}
    type: DirectoryOrCreate

---
# PersistentVolumeClaim for Attraction
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${ATTRACTION_TYPE}-pvc-${INSTANCE_ID}
  namespace: attractions
  labels:
    app.kubernetes.io/name: kubepark
    app.kubernetes.io/component: attraction
    app.kubernetes.io/instance: ${ATTRACTION_TYPE}-${INSTANCE_ID}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${ATTRACTION_TYPE}-${INSTANCE_ID}
  namespace: attractions
  labels:
    app.kubernetes.io/name: kubepark
    app.kubernetes.io/component: attraction
    app.kubernetes.io/instance: ${ATTRACTION_TYPE}-${INSTANCE_ID}
    app: ${ATTRACTION_TYPE}-${INSTANCE_ID}
    attraction: ${ATTRACTION_TYPE}
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: ${ATTRACTION_TYPE}-${INSTANCE_ID}
  template:
    metadata:
      labels:
        app: ${ATTRACTION_TYPE}-${INSTANCE_ID}
        app.kubernetes.io/name: kubepark
        app.kubernetes.io/component: attraction
        app.kubernetes.io/instance: ${ATTRACTION_TYPE}-${INSTANCE_ID}
        attraction: ${ATTRACTION_TYPE}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9000"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        fsGroup: 1000
      initContainers:
        - name: volume-permissions
          image: busybox:1.35
          command:
            ["sh", "-c", "chown -R 1000:1000 /data && chmod -R 755 /data"]
          volumeMounts:
            - name: ${ATTRACTION_TYPE}-storage-${INSTANCE_ID}
              mountPath: /data
          securityContext:
            runAsUser: 0
      containers:
        - name: ${ATTRACTION_TYPE}
          image: localhost:5001/kubepark:latest
          ports:
            - containerPort: 80
              name: http
            - containerPort: 9000
              name: metrics
          command: ["${ATTRACTION_TYPE}"]
          args:
            - "--park-url"
            - "http://park.park.svc.cluster.local."
            - "--volume"
            - "/data"
          volumeMounts:
            - name: ${ATTRACTION_TYPE}-storage-${INSTANCE_ID}
              mountPath: /data
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
          livenessProbe:
            httpGet:
              path: /attraction-status
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /attraction-status
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: ${ATTRACTION_TYPE}-storage-${INSTANCE_ID}
          persistentVolumeClaim:
            claimName: ${ATTRACTION_TYPE}-pvc-${INSTANCE_ID}
---
apiVersion: v1
kind: Service
metadata:
  name: ${ATTRACTION_TYPE}-${INSTANCE_ID}
  namespace: attractions
  labels:
    app.kubernetes.io/name: kubepark
    app.kubernetes.io/component: attraction
    app.kubernetes.io/instance: ${ATTRACTION_TYPE}-${INSTANCE_ID}
    app: ${ATTRACTION_TYPE}-${INSTANCE_ID}
    attraction: ${ATTRACTION_TYPE}
spec:
  selector:
    app: ${ATTRACTION_TYPE}-${INSTANCE_ID}
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
    - name: metrics
      protocol: TCP
      port: 9000
      targetPort: 9000
  type: ClusterIP
